# The name of the folder which will contain your site on the server
site: <%= site_name %>

ssh:
  ip: # 0.0.0.0
  user: deploy
  # keys: [ "path/to/key" ]       # Optional
  # port: 22                      # Optional

services:
  static_site:
    domain: example.com
    mechanism: rsync
  # web:                          # Optional
  #   domain: api.example.com
  #   mechanism: git
