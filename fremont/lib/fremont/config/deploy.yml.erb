site: <%= site_name %>

ssh:
  ip:
  user: deploy
  keys: [ key ]
  port: 22

services:
  static_site:
    domain: example.com
    mechanism: rsync
  # web:
  #   domain: api.example.com
  #   mechanism: git
